toggle:
  accountLists:
    1.enabled: true
  transactionLists:
    1.enabled: true

Spring:
  flyway:
    locations: db/migration/local
  datasource:
    url: jdbc:postgresql://localhost:5432/wholesale
    username: wholesale
    password: wholesale
    driver-class-name: org.postgresql.Driver
